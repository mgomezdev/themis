services:
  themis:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - themis-data:/data
      - "${APPDATA}/OrcaSlicer:/root/.config/OrcaSlicer:ro"
    restart: unless-stopped

volumes:
  themis-data:
